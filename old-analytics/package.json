{
  "name": "@pi/analytics",
  "version": "0.1.0",
  "description": "Usage analytics extension for pi — captures tokens, cost, tools, latency, git context per session.",
  "type": "module",
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test src/**/*.test.ts"
  },
  "dependencies": {
    "@mariozechner/pi-tui": "^0.73.0",
    "better-sqlite3": "^12.9.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
